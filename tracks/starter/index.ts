import { LightrailTrack } from "lightrail-sdk";

declare function require(module: string): any;

export default {
  name: "CHANGE ME", // Everything except name is optional
  tokens: [],
  actions: [],
  handlers: {
    main: {},
    renderer: {},
  },
} satisfies LightrailTrack;
